<% if signed_in? %>
        <h1>Welcome, <%= current_user.firstname %> <%= current_user.lastname %> !</h1>
    <% else %>
    <h1>Please, Sign In or Sign Up</h1>
			 <video width="800" height="600" poster="/images/poster.jpg" controls>
			    <source src="FindSomeone.mp4" type="video/mp4">
			 </video>

    <% end %>


